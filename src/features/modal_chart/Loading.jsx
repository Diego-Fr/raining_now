const Loading = options =>{
    return (
        <div style={{width: '100%', height: '100%', backgroundColor: 'white', fontWeight: 600, display: 'flex', alignItems:'center', justifyContent: 'center'}}>
            CARREGANDO DADOS
        </div>
    )
}

export default Loading